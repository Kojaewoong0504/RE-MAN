# required-checks.md

## Purpose

이 문서는 GitHub에서 required check로 지정해야 하는 워크플로 이름을 명시한다.

## Required Checks

아래 이름을 required check로 둔다.

1. `required-gates`
2. `onboarding-e2e`

## Rule

브랜치 보호 설정은 GitHub 저장소 설정에서 적용해야 한다.
하지만 required check 이름은 이 문서와 workflow에서 고정한다.
